// swift-tools-version:5.9
// used for intellisense only: we will prefer `make`

import PackageDescription

let package = Package(
    name: "trackpaddy",
    platforms: [
        .macOS(.v13)
    ]
)
